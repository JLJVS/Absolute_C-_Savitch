/*

When you you define a class in separate files, there is an interface file and an implementation file. Which of these files remains as a text file and need not be compiled?

The interface file remains as text since it gets copy pasted when it encounters #include "file.h"", thus it can remain a text file.

*/
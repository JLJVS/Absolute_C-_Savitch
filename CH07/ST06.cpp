/*

const int fees = 2555;
that int won't be changed

class School
{
    public:
        School ( );
        School (int n);
        int findout ( ) const; the promise that findout won't change the school class/instance
        int generate(const School& fees); // that the argument won't be changed.
    private:
        int i;

};


*/
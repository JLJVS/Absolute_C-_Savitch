#include <iostream>
using namespace std;

/*

void BankAccount::input(){
    int dollars;
    char point, d1, d2;

    cout << "Enter account balance";
    cin >> dollars;
    cin >> point >> d1 >> d2;
    accountDollars = dollars;
    accountCents = (static_cast<int>(d1)-static_cast<int>('0'))*10 + (static_cast<int>(d2)-static_cast<int>('0'));

    if (accountDollars < 0){
        accountCents -= accountCents;    
    }
    
    cout << "Enter interest rate:";
    cin >> rate;
    setRate(rate);

}

*/  
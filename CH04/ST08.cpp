/*
two prototypes
double score(double time, double distance);
int score(double points);

double x, y;
double finaleScore = score(x,y);

it will use the first prototype, the return type matches and it has two parameters (of the correct type)

*/
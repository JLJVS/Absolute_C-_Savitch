#include <iostream>
#include <cmath>
using namespace std;

int main()
{   
    float x{2}, y{3};
    // a 
    cout << "a: " << sqrt(x+y) << endl;

    // b
    cout << "b: " << pow(x, y+7) << endl;

    // c
    // cout << "c: " << sqrt(area + fudge);

    // d
    // cout << sqrt(time + tide)/ nobody

    // e
    // -b +/- sqrt(b*b - 4*a*c)/(2*a)

    // f
    cout << "f: " << abs(x-y) << endl;
    return 0;
}
/*

We can use operator overloading to define behaviour for custom classes 

e.g. we define a point in a 2d space class with Point(int x, int y)
We need to define what would happen if we add two points together
P1(x1, y1) + P2(x2, y2) = P3(x1+x2, y1+2)
this is behaviour we need to define with operator overloading

*/
/*

A friend function is defined and used just like an ordinary function.

example
Money(100, 60) + 25 = Money(125, 60)
the + is overloaded to be a friend function so it can check if the class has a constructor with just an int as a parameter.

*/